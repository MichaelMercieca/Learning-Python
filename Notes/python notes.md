# Python Notes:

- Use Arrays over Lists where possible
- Use list/dict comprehensiosn whenever possible
- Use generators where possible to save memory
- Use keyword arguments to reduce mistakes
- Always enforce types in functions#
- Use ** to unpack dictionary contents
- Define and use Constants where possible
- Format floats when printing to decimal places ex: .4f
- def main() -> None:
- Never use mutable defaults like lists in class __init__s
- 'if not something' and 'if something is x'
- use @classmethod and cls where makes sense
- Transition mapping for Enums
- Use descriptors when many classes genuinely share exactly the same validation behaviour
    - otherwise just use properties for attribute validation 

- use Broadcasting in NumPy
